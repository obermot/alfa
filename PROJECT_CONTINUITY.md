# Незабудка — постоянное состояние проекта и правила продолжения

Этот файл является долговечным источником контекста для следующих чатов, handoff-пакетов и исполнителей. При продолжении разработки его нужно читать до внесения существенных изменений.

## 1. Рабочий репозиторий

- Канонический рабочий репозиторий: `obermot/alfa`.
- Репозиторий публичный.
- Старый `obermot/Nezabudka` не используется как рабочая база, но его история может использоваться как источник проверенных прошлых состояний при recovery/regression-анализе.
- В публичный репозиторий нельзя добавлять keystore, пароли, токены, секреты, приватные handoff-файлы и иные чувствительные материалы.

## 2. Текущее приложение

- Android applicationId: `com.nezabudka.alpha.clean013`.
- Текущая версия в `main`: `0.2.0-alarm-screen-alpha`, versionCode `110`.
- Базовое пользовательское ядро `MainActivity` восстановлено из проверенной 0.1.5.
- APK предназначен для установки и тестирования на физическом Android-смартфоне.

## 3. GitHub Actions — постоянное правило экономии

GitHub Actions используется только для осмысленных контрольных сборок.

- Обычные коммиты в `main` не должны автоматически запускать сборку.
- Контрольная сборка вызывается отдельно через узкий триггер `ci/BUILD_REQUEST` либо вручную через workflow dispatch.
- Перед запуском Actions следует пакетировать связанные изменения и делать один контрольный build.
- Нельзя перезапускать workflow вслепую. При ошибке сначала анализируется причина.
- Чтение статуса, логов, артефактов и репозитория не является поводом для нового запуска.
- Существующий успешный build можно использовать как подтверждение собираемости неизменённого состояния.

Workflow: `.github/workflows/android-build.yml`.

## 4. Постоянная подпись APK

Начиная с контрольной сборки 2026-09-19 введена постоянная тестовая подпись APK.

Цель: все последующие тестовые APK должны устанавливаться поверх предыдущей версии без конфликта подписи и без удаления приложения.

### 4.1. Хранение ключа

- Сам keystore НЕ хранится в Git-репозитории.
- Резервная копия keystore хранится у владельца проекта локально.
- `.gitignore` блокирует `*.jks`, `*.keystore`, `*.p12`, `*.pem`, `*.key`, `.env*`, `secrets.*`, `credentials.*`, `keystore.properties`, `signing.properties` и другие чувствительные файлы.
- Значения секретов никогда не записывать в этот файл, issue, commit, PR, workflow log или публичный код.

### 4.2. GitHub Repository Secrets

Для подписи используются четыре Repository Secrets:

- `NEZABUDKA_KEYSTORE_B64`
- `NEZABUDKA_KEYSTORE_PASSWORD`
- `NEZABUDKA_KEY_ALIAS`
- `NEZABUDKA_KEY_PASSWORD`

Имена можно хранить публично; значения — только в GitHub Secrets и резервной копии владельца проекта.

### 4.3. Механизм сборки

- Workflow восстанавливает keystore из `NEZABUDKA_KEYSTORE_B64` только во временной среде GitHub runner.
- Gradle получает путь к восстановленному keystore и параметры подписи через переменные окружения / secrets.
- `app/build.gradle.kts` содержит явный signingConfig для постоянной тестовой подписи.
- Если секреты подписи отсутствуют в обычной локальной среде, локальная debug-сборка не должна раскрывать или требовать секреты из репозитория.

### 4.4. Проверка

Контрольная сборка GitHub Actions №3 (`ci: verify persistent APK signing`) успешно завершилась 2026-09-19.

Успешно прошли шаги:

- Restore signing keystore
- Build signed debug APK
- Upload artifact

Это подтверждает, что Base64 keystore, пароль хранилища, alias и пароль ключа настроены корректно.

### 4.5. Правило для будущих версий

- Нельзя менять signing key без отдельного осознанного решения владельца проекта.
- Все новые тестовые APK из `obermot/alfa` должны подписываться этим же постоянным ключом.
- Потеря этого ключа означает потерю возможности обновлять уже установленную тестовую ветку тем же package ID без удаления приложения.

## 5. Текущий цикл доставки тестовой версии

1. Изменения вносятся в `obermot/alfa` без лишних Actions.
2. Связанные изменения накапливаются в один осмысленный тестовый пакет.
3. Выполняется одна контрольная сборка через `ci/BUILD_REQUEST`.
4. Из GitHub Actions забирается APK-артефакт.
5. APK передаётся пользователю в текущем чате.
6. Пользователь открывает тот же чат на смартфоне, скачивает APK и устанавливает его.
7. Пользователь тестирует и сообщает фактическое поведение.

USB, Android Studio и локальная сборка на ноутбуке для обычного тестового цикла не требуются.

## 6. Open Source Search Gate

Перед добавлением нетривиальной новой возможности нужно сначала проверить существующие зрелые open-source решения / библиотеки / SDK / reference implementations.

При сравнении учитывать минимум: качество, лицензию, безопасность, приватность, Android/Kotlin-совместимость, размер зависимости, поддержку, офлайн-возможности и стоимость эксплуатации.

Нельзя подключать тяжёлую зависимость только потому, что она существует. Если текущая лёгкая реализация объективно лучше для приложения, это допустимое решение.

## 7. Проверенная база 0.1.5

Последняя версия, на которой пользователь фактически подтвердил создание и срабатывание напоминаний до последующих регрессий: `0.1.5-clean-alpha` из старого репозитория `obermot/Nezabudka`, ветка `test-harness`.

Контрольная сборка 0.1.5: commit `6be2936f571cc11e1383f1693aa60c1ee9214755`.
Кодовое состояние перед build-trigger: commit `79711e23540dc733cc73b9b989c093f7204a61fd`.

`MainActivity.kt` из этой версии имеет blob SHA `a9ff81894e81c96ff6faa8bf5f0df6426f50653a`.

## 8. Регрессия 0.1.7 / 0.1.8 и recovery 0.1.9

Фактический тест подписанной 0.1.7 показал:

- имя голосом распознаётся и изменяется;
- основной голосовой ввод напоминаний после ошибки распознавания перестаёт надёжно работать;
- текстовый ввод напоминаний не приводит к созданию reminder;
- в итоге пользователь не смог создать ни одного напоминания ни голосом, ни текстом.

Попытка 0.1.8 переразделить режим имени и пересоздавать `SpeechRecognizer` признана промежуточной и НЕ считается новой рабочей базой. 0.1.8 не использовать как reference-state для дальнейшего развития.

По решению пользователя выполнен контролируемый rollback пользовательского ядра:

- `MainActivity.kt` в `0.1.9-recovery-alpha` восстановлен бит-в-бит из проверенной 0.1.5;
- восстановленный blob SHA: `a9ff81894e81c96ff6faa8bf5f0df6426f50653a`;
- постоянная подпись APK, public repository, GitHub Secrets, экономный workflow и проектная документация сохранены.

Фактический тест 0.1.9 подтвердил:

- текстовое напоминание создаётся и срабатывает;
- голосовое напоминание создаётся, но первая попытка распознавания может быть нестабильной;
- приложение помнит имя;
- возможность редактировать имя отсутствует, потому что recovery вернул UI 0.1.5;
- фраза `через полминуты` не распознавалась, тогда как `через минуту` распознавалась;
- приложение произносило `Вы услышали?`, но голосовой ответ не слушало.

`через полминуты` исправлено как относительное время 30 секунд.

## 9. Alarm Screen / голосовое подтверждение

По решению пользователя введён отдельный видимый `AlarmActivity`, который в дальнейшем может стать полноценным тревожным экраном с назначаемыми цветами и дополнительными режимами.

Текущая архитектура:

- при срабатывании reminder уведомление получает `full-screen intent` на `AlarmActivity`;
- в manifest объявлен `USE_FULL_SCREEN_INTENT`;
- экран может показываться поверх lock screen и включать экран;
- если Android не разрешит автоматический full-screen запуск, нажатие на alarm-уведомление открывает тот же `AlarmActivity`;
- `AlarmReceiver` по-прежнему воспроизводит alarm sound, имя, естественный текст reminder и вопрос `Вы услышали?`;
- после завершения TTS `AlarmReceiver` посылает видимому `AlarmActivity` сигнал начать короткое голосовое распознавание;
- подтверждения вроде `да`, `услышал`, `понял`, `готово`, `всё`, `хватит`, `не напоминай`, `я уже встал` и т. п. переводятся в штатный `ACTION_ACK`;
- на экране всегда есть гарантированные кнопки `Ответить голосом`, `Услышал / выключить`, `+10 минут`;
- каждая попытка голосового ответа на alarm-screen создаёт новый экземпляр Android `SpeechRecognizer`;
- после ошибки голосового ответа пользователь может повторить его кнопкой `Ответить голосом`.

Причина видимого alarm-screen: на современных Android `RECORD_AUDIO` — while-in-use permission; скрытое фоновое включение микрофона после alarm ненадёжно и ограничено системой. Голосовой ответ следует слушать, когда activity видима.

Open Source / Android Search Gate для этой функции выполнен; выбрана штатная Android архитектура `full-screen intent + visible Activity + SpeechRecognizer`, без новой тяжёлой зависимости.

## 10. Незавершённые ближайшие задачи

После теста 0.2.0:

- подтвердить, появляется ли AlarmActivity автоматически на конкретном смартфоне;
- подтвердить голосовое ACK после `Вы услышали?`;
- при необходимости провести пользователя через системное разрешение full-screen alarm на Android 14+;
- отдельно стабилизировать обычную кнопку `Говорить` в MainActivity через fresh SpeechRecognizer, но не переписывать recovery-ядро целиком;
- затем вернуть редактирование имени и другие полезные UI-возможности поверх подтверждённой базы.

## 11. Правило handoff

Каждый следующий handoff должен явно передавать как минимум:

- рабочий репозиторий `obermot/alfa`;
- публичный статус репозитория;
- указание на `PROJECT_CONTINUITY.md` как обязательный источник контекста;
- правило экономии GitHub Actions;
- правило Open Source Search Gate;
- наличие постоянной APK-подписи;
- имена четырёх signing secrets, но НИКОГДА не их значения;
- запрет менять signing key без явного решения;
- текущую версию приложения и последний успешный контрольный build;
- проверенную recovery-базу 0.1.5 и факт rollback `MainActivity` в 0.1.9;
- архитектуру Alarm Screen и ограничения фонового микрофона;
- фактический результат последнего тестирования пользователя и следующий незавершённый шаг.

Если состояние проекта меняется существенно, этот файл следует обновлять тем же рабочим циклом, не оставляя изменения только в памяти чата.
